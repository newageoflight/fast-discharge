import React from 'react'

// "Wizard"-style template that allows you to populate a field using a wizard in a modal or sidebar div
// You basically just click on it and it will open a sidebar or modal to replace itself with text

export const RefWizardTemplate: React.FC = () => {
    return (
        <div>
            
        </div>
    )
}
