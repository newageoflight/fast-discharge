import React from 'react'

// "Wizard"-style template that allows you to populate the editor using a form in a modal or sidebar div
// For example, you can tick boxes about "abdominal pain" and it'll use logic to decide what to insert
// This will be the hardest type to implement since it's essentially allowing users to create their own forms
// And then linking that form to a logic about what to insert into the editor

export const WizardTemplate: React.FC = () => {
    return (
        <div>
            
        </div>
    )
}
