import { SlatePlugin } from '@udecode/slate-plugins';

// again, have a look at the mentions plugin before you get deep into this

interface FragmentInserterOptions {

}

export const FragmentInserterPlugin = (options?: FragmentInserterOptions): SlatePlugin => ({
    
})